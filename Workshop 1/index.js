// Check Age for Voting Eligibility

let age = 17;

console.log(age >= 18 ? "The Person can vote" : "The Person can't vote")

if (age >= 18){
    console.log("The Person can vote")
} else {
    console.log("The Person can't vote")
}